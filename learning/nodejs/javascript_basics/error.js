// Handling Errors (Try-Catch)

// Proper error handling is crucial for dealing with runtime issues.

try {
    let x = y; // y is not defined
  } catch (error) {
    console.log("Error caught:", error.message);
  }
  